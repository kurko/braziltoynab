# frozen_string_literal: true

RSpec.describe Portoseguro::Ynab do
  it "has a version number" do
    expect(Portoseguro::Ynab::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
